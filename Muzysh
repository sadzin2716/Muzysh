--[[
	Muzysh UI Library - Refatorada e Otimizada
	Features: Responsivo, Animações Modernas, Cleanup Automático, Modular.
]]

local Muzysh = {}
Muzysh.__index = Muzysh

-- Services
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Theme (extendable)
local Theme = { ... }

-- Utility functions
local function createTween(instance, tweenInfo, properties, callback) ... end
local function animate(instance, duration, style, properties) ... end -- wrapper with common easing

-- Drag Handler Class
local DragHandler = {}
DragHandler.__index = DragHandler
function DragHandler.new(dragFrame, container) ... end -- returns object with :StartDrag(input) and :StopDrag(), handles UIS

-- Cleanup class (Janitor)
local Janitor = {}
function Janitor.new() ... end -- add connection, instance, function to cleanup

-- Component modules: Button, Toggle, Slider, Dropdown, TextBox, Section
-- Each returns a builder function that takes parent, theme, cleanup.

-- Main Window class
function Muzysh.Init(title)
	local self = setmetatable({}, Muzysh)
	self._cleanup = Janitor.new()
	-- create ScreenGui, main frame, scaling
	-- ...
	-- TopBar, Sidebar, PageContainer
	--...
	return self
end

function Muzysh:CreatePage(pageName)
	-- returns page object with methods to add components
end

-- Expose components as properties for custom themes maybe.
Muzysh.Theme = Theme

return Muzysh
